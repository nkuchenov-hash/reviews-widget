(function(){
  'use strict';
  const D={layout:'slider',columns:3,gap:18,radius:18,maxWidth:1200,fontFamily:'Inter,Arial,sans-serif',accent:'#111111',background:'#ffffff',cardBackground:'#ffffff',textColor:'#171717',mutedColor:'#777777',borderColor:'#e7e7e7',minRating:1,reviewLimit:12,sort:'newest',showAvatar:true,showDate:true,showSource:true,showRating:true,showSummary:true,showReviewButton:true,showControls:true,autoplay:true,autoplayDelay:4500,loop:true,yandexHeight:620};
  const C={title:'Отзывы клиентов',subtitle:'Отзывы с независимых площадок',buttonText:'Оставить отзыв',buttonUrl:'',showHeader:true};
  const sourceLabels={yandex:'Яндекс Карты',google:'Google',twogis:'2ГИС',custom:'Отзыв'};
  function esc(s){return String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}
  function safeUrl(u){try{const x=new URL(u,location.href);return ['http:','https:'].includes(x.protocol)?x.href:'#'}catch{return '#'}}
  function dateLabel(v){if(!v)return'';const d=new Date(v);return isNaN(d)?'':new Intl.DateTimeFormat('ru-RU',{day:'numeric',month:'long',year:'numeric'}).format(d)}
  function stars(n){const r=Math.round(Number(n)||0);return `<span class="urw-stars" aria-label="${r} из 5">${'★'.repeat(r)}${'☆'.repeat(5-r)}</span>`}
  function initials(s){return esc(String(s||'К').trim().split(/\s+/).slice(0,2).map(x=>x[0]).join('').toUpperCase())}
  function inject(){if(document.getElementById('urw-v2-style'))return;const s=document.createElement('style');s.id='urw-v2-style';s.textContent=`
  .urw{--u-font:Inter,Arial,sans-serif;--u-accent:#111;--u-bg:#fff;--u-card:#fff;--u-text:#171717;--u-muted:#777;--u-border:#e7e7e7;--u-radius:18px;--u-gap:18px;--u-cols:3;--u-max:1200px;font-family:var(--u-font);color:var(--u-text);background:var(--u-bg);width:100%}.urw *{box-sizing:border-box}.urw-shell{max-width:var(--u-max);margin:auto;padding:24px 0}.urw-head{display:flex;justify-content:space-between;gap:24px;align-items:end;margin-bottom:24px}.urw-kicker{font-size:13px;color:var(--u-muted);margin-bottom:6px}.urw-title{font-size:34px;line-height:1.05;letter-spacing:-.04em;margin:0}.urw-summary{display:flex;align-items:center;gap:8px;margin-top:12px;font-size:13px;color:var(--u-muted)}.urw-score{font-weight:800;color:var(--u-text);font-size:20px}.urw-stars{letter-spacing:1px;color:#f2b01e;white-space:nowrap}.urw-btn{display:inline-flex;align-items:center;justify-content:center;min-height:42px;padding:0 16px;border:1px solid var(--u-accent);background:var(--u-accent);color:#fff;text-decoration:none;border-radius:999px;font-size:14px;font-weight:700}.urw-viewport{overflow:hidden}.urw-track{display:flex;gap:var(--u-gap);transition:transform .35s ease}.urw-track .urw-card{min-width:calc((100% - (var(--u-cols) - 1)*var(--u-gap))/var(--u-cols))}.urw-grid{display:grid;grid-template-columns:repeat(var(--u-cols),minmax(0,1fr));gap:var(--u-gap)}.urw-list{display:grid;grid-template-columns:1fr;gap:var(--u-gap)}.urw-card{border:1px solid var(--u-border);border-radius:var(--u-radius);background:var(--u-card);padding:20px;min-height:235px;display:flex;flex-direction:column}.urw-top{display:flex;align-items:center;gap:11px}.urw-avatar{width:42px;height:42px;border-radius:50%;overflow:hidden;background:#f0f0f0;display:grid;place-items:center;font-size:13px;font-weight:800;flex:0 0 auto}.urw-avatar img{width:100%;height:100%;object-fit:cover}.urw-author{font-weight:750;font-size:14px}.urw-meta{font-size:12px;color:var(--u-muted);margin-top:3px}.urw-source{margin-left:auto;font-size:11px;color:var(--u-muted);text-decoration:none}.urw-rating{margin:16px 0 10px;font-size:14px}.urw-text{font-size:15px;line-height:1.55;margin:0;white-space:pre-line}.urw-foot{display:flex;justify-content:space-between;gap:12px;margin-top:auto;padding-top:18px}.urw-read{font-size:12px;color:var(--u-text);text-decoration:none}.urw-controls{display:flex;justify-content:flex-end;gap:8px;margin-top:16px}.urw-arrow{width:42px;height:42px;border-radius:50%;border:1px solid var(--u-border);background:var(--u-card);color:var(--u-text);cursor:pointer}.urw-empty{padding:32px;border:1px dashed var(--u-border);border-radius:var(--u-radius);text-align:center;color:var(--u-muted)}.urw-yandex-wrap{overflow:hidden;border:1px solid var(--u-border);border-radius:var(--u-radius);background:var(--u-card)}.urw-yandex{display:block;width:100%;border:0;min-height:500px}.urw-yandex-credit{display:block;text-align:center;color:var(--u-muted);font-size:10px;padding:7px 10px;text-decoration:none}.urw-live{display:inline-flex;align-items:center;gap:6px;font-size:12px;color:var(--u-muted);margin-top:10px}.urw-live:before{content:'';width:7px;height:7px;border-radius:50%;background:#33a852}.urw-note{font-size:12px;color:var(--u-muted);margin-top:10px}
  @media(max-width:900px){.urw{--u-cols:2}.urw-title{font-size:30px}}@media(max-width:640px){.urw{--u-cols:1}.urw-head{display:grid}.urw-track .urw-card{min-width:100%}.urw-grid{grid-template-columns:1fr}}
  `;document.head.appendChild(s)}
  class W{
    constructor(el,c){this.el=el;this.raw=c||{};this.i=0;this.timer=null;inject();this.init()}
    async init(){let payload=this.raw;if(this.raw.widgetId||this.raw.apiUrl){const url=this.raw.apiUrl||`${this.raw.baseUrl||''}/api/widget/${encodeURIComponent(this.raw.widgetId)}`;const r=await fetch(url,{headers:{Accept:'application/json'}});if(!r.ok)throw new Error(`Reviews widget HTTP ${r.status}`);payload=await r.json()}this.payload=payload;this.design={...D,...(payload.design||{}),...(this.raw.design||{})};this.content={...C,...(payload.content||{}),...(this.raw.content||{})};this.apply();this.render();this.start()}
    apply(){const d=this.design,s=this.el.style;this.el.className='urw';s.setProperty('--u-font',d.fontFamily);s.setProperty('--u-accent',d.accent);s.setProperty('--u-bg',d.background);s.setProperty('--u-card',d.cardBackground);s.setProperty('--u-text',d.textColor);s.setProperty('--u-muted',d.mutedColor);s.setProperty('--u-border',d.borderColor);s.setProperty('--u-radius',`${Number(d.radius)}px`);s.setProperty('--u-gap',`${Number(d.gap)}px`);s.setProperty('--u-cols',Math.max(1,Number(d.columns)||1));s.setProperty('--u-max',`${Number(d.maxWidth)}px`)}
    header(summaryHtml=''){const c=this.content;if(c.showHeader===false)return'';return `<div class="urw-head"><div><div class="urw-kicker">${esc(c.subtitle)}</div><h2 class="urw-title">${esc(c.title)}</h2>${summaryHtml}</div>${this.design.showReviewButton&&c.buttonUrl?`<a class="urw-btn" href="${esc(safeUrl(c.buttonUrl))}" target="_blank" rel="noopener">${esc(c.buttonText)}</a>`:''}</div>`}
    render(){if(this.payload.mode==='yandex-live')return this.renderYandex();this.renderManaged()}
    renderYandex(){const p=this.payload,d=this.design,id=String(p.source?.businessId||'');const src=`https://yandex.ru/maps-reviews-widget/${encodeURIComponent(id)}?comments`;const org=safeUrl(p.source?.businessUrl||`https://yandex.ru/maps/org/${encodeURIComponent(id)}/reviews/`);this.el.innerHTML=`<div class="urw-shell">${this.header(`<div class="urw-live">Обновляется напрямую из Яндекс Карт</div>`)}<div class="urw-yandex-wrap"><iframe class="urw-yandex" style="height:${Math.max(500,Number(d.yandexHeight)||620)}px" loading="lazy" src="${esc(src)}"></iframe><a class="urw-yandex-credit" href="${esc(org)}" target="_blank" rel="noopener">Открыть организацию на Яндекс Картах ↗</a></div></div>`}
    prep(){const d=this.design;let a=(this.payload.reviews||[]).filter(r=>Number(r.rating||0)>=Number(d.minRating||0));if(d.sort==='newest')a.sort((x,y)=>new Date(y.date||0)-new Date(x.date||0));if(d.sort==='oldest')a.sort((x,y)=>new Date(x.date||0)-new Date(y.date||0));if(d.sort==='rating')a.sort((x,y)=>Number(y.rating||0)-Number(x.rating||0));if(d.sort==='random')a.sort(()=>Math.random()-.5);a.sort((x,y)=>Number(Boolean(y.pinned))-Number(Boolean(x.pinned))||(Number(x.pinOrder)||0)-(Number(y.pinOrder)||0));return a.slice(0,Math.max(1,Number(d.reviewLimit)||12))}
    card(r){const d=this.design;return `<article class="urw-card"><div class="urw-top">${d.showAvatar?`<div class="urw-avatar">${r.avatar?`<img src="${esc(safeUrl(r.avatar))}" alt="">`:initials(r.author)}</div>`:''}<div><div class="urw-author">${esc(r.author||'Клиент')}</div>${d.showDate?`<div class="urw-meta">${esc(dateLabel(r.date))}</div>`:''}</div>${d.showSource?`<span class="urw-source">${esc(sourceLabels[r.source]||r.source||'Отзыв')}</span>`:''}</div>${d.showRating?`<div class="urw-rating">${stars(r.rating)}</div>`:''}<p class="urw-text">${esc(r.text||'')}</p><div class="urw-foot"><span class="urw-meta">${r.pinned?'Закреплённый отзыв':''}</span>${r.url?`<a class="urw-read" href="${esc(safeUrl(r.url))}" target="_blank" rel="noopener">Оригинал ↗</a>`:''}</div></article>`}
    renderManaged(){this.reviews=this.prep();const avg=this.reviews.length?(this.reviews.reduce((s,r)=>s+Number(r.rating||0),0)/this.reviews.length).toFixed(1):'0.0';const summary=this.design.showSummary?`<div class="urw-summary"><span class="urw-score">${avg}</span>${stars(avg)}<span>${this.reviews.length} отзывов</span></div>`:'';const cls=this.design.layout==='grid'?'urw-grid':this.design.layout==='list'?'urw-list':'urw-track';this.el.innerHTML=`<div class="urw-shell">${this.header(summary)}${this.reviews.length?`<div class="${this.design.layout==='slider'?'urw-viewport':''}"><div class="${cls}" data-track>${this.reviews.map(r=>this.card(r)).join('')}</div></div>`:`<div class="urw-empty">Нет отзывов для выбранного фильтра.</div>`}${this.design.layout==='slider'&&this.design.showControls&&this.reviews.length>1?`<div class="urw-controls"><button class="urw-arrow" data-prev>←</button><button class="urw-arrow" data-next>→</button></div>`:''}</div>`;if(this.design.layout==='slider')this.bind()}
    visible(){const w=this.el.getBoundingClientRect().width;return w<=640?1:w<=900?Math.min(2,Number(this.design.columns)||2):Math.max(1,Number(this.design.columns)||3)}
    max(){return Math.max(0,(this.reviews?.length||0)-this.visible())}
    bind(){this.track=this.el.querySelector('[data-track]');this.el.querySelector('[data-prev]')?.addEventListener('click',()=>this.go(this.i-1,true));this.el.querySelector('[data-next]')?.addEventListener('click',()=>this.go(this.i+1,true));this.go(0)}
    go(n,user=false){if(!this.track)return;const max=this.max();if(this.design.loop){if(n<0)n=max;if(n>max)n=0}else n=Math.max(0,Math.min(max,n));this.i=n;const first=this.track.children[0];if(first)this.track.style.transform=`translate3d(-${(first.getBoundingClientRect().width+Number(this.design.gap||18))*n}px,0,0)`;if(user){this.stop();this.start()}}
    start(){this.stop();if(this.payload.mode!=='managed'||this.design.layout!=='slider'||!this.design.autoplay||this.max()===0)return;this.timer=setInterval(()=>this.go(this.i+1),Math.max(1500,Number(this.design.autoplayDelay)||4500))}
    stop(){if(this.timer)clearInterval(this.timer);this.timer=null}
  }
  window.UniversalReviews={mount(target,c){const el=typeof target==='string'?document.querySelector(target):target;if(!el)throw new Error('Widget target not found');return new W(el,c)},mountAll(){document.querySelectorAll('[data-universal-reviews]').forEach(el=>{let c={};try{c=JSON.parse(el.getAttribute('data-config')||'{}')}catch{};window.UniversalReviews.mount(el,c)})}};
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>window.UniversalReviews.mountAll());else window.UniversalReviews.mountAll();
})();